# Copy this file to .env and adjust as needed.
PORT=5000
# 5 minutes in milliseconds. Lower this while testing to see
# reservations expire faster (e.g. 30000 for 30 seconds).
RESERVATION_TIMEOUT_MS=300000
